{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.12.0"
  },
  "scripts": {
    "build": "turbo run build --filter=@autospace/web-admin"
  }
}
