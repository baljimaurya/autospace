{
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.12.0"
  },
  "scripts": {
    "build:web": "turbo run build --filter=@autospace/web",
    "build:manager": "turbo run build --filter=@autospace/web-manager",
    "build:valet": "turbo run build --filter=@autospace/web-valet",
    "build:admin": "turbo run build --filter=@autospace/web-admin"
  }
}
