{
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.12.0"
  },
  "scripts": {
    "build": "turbo run build --filter=@autospace/web-admin"
  }
}
